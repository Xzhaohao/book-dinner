<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 下边线 -->
    <style name="default_line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
        <item name="android:background">@color/bottom_line</item>
    </style>

    <!-- primary按钮 -->
    <style name="primary_button">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">46dp</item>
        <item name="android:background">@drawable/primary_button</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- 输入框 -->
    <style name="input">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">60dp</item>
        <item name="android:layout_marginEnd">60dp</item>
        <item name="android:background">@null</item>
        <item name="android:paddingStart">5dp</item>
        <item name="android:paddingEnd">5dp</item>
        <item name="android:textColorHint">@color/black_tag</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- ActionBar标题 -->
    <style name="action_bar_title">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/blackText3</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!-- ActionBar返回图标 -->
    <style name="action_bar_back">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginStart">15dp</item>
        <item name="android:src">@mipmap/back_icon</item>
    </style>

    <!-- 小菜单 -->
    <style name="main_menu_item">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:textColor">@color/blackText3</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- 单选框 -->
    <style name="radio_button">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:drawableStart">@drawable/radio_button_selector</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:textColor">@color/blackText3</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!-- 搜索框 -->
    <style name="search_input">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">20dp</item>
        <item name="android:layout_marginEnd">20dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:drawableStart">@drawable/nav_icon_search</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:hint">@string/toolbar_input_hint</item>
        <item name="android:inputType">text</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColorHint">#9C9C9C</item>
        <item name="android:background">@drawable/search_input_bg</item>
    </style>

    <!-- 首页小菜单 -->
    <style name="home_menu_item">
        <item name="android:layout_columnWeight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:textColor">@color/blackText3</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- 首页排序tabs -->
    <style name="home_sort_tabs">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/gray_6</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>